spring.application.name=cinema-monarca
server.port=${PORT:8080}

# ── MySQL Railway ──
spring.datasource.url=jdbc:mysql://${MYSQLHOST}:${MYSQLPORT}/${MYSQLDATABASE}?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&characterEncoding=UTF-8
spring.datasource.username=${MYSQLUSER}
spring.datasource.password=${MYSQLPASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ── Hikari Pool (reducido para Railway free tier) ──
spring.datasource.hikari.maximum-pool-size=3
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=600000
spring.datasource.hikari.leak-detection-threshold=60000

# ── JPA / Hibernate ──
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.open-in-view=false

# ── Jackson ──
spring.jackson.default-property-inclusion=non_null

# ── SQL init ──
spring.sql.init.mode=always
spring.sql.init.continue-on-error=true

# ── JWT ──
app.jwt.secret=CinemaMonarcaSecretKeyMustBe32CharsLongABC
app.jwt.expiration-ms=86400000

# ── CORS ──
spring.web.cors.allowed-origins=*

# ── Logging (minimo para ahorrar memoria) ──
logging.level.com.cinemamonarca=WARN
logging.level.org.springframework.security=WARN
logging.level.org.hibernate.SQL=WARN
logging.level.org.springframework.web=WARN
logging.level.root=WARN

# ── Swagger UI ──
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.try-it-out-enabled=true
springdoc.swagger-ui.operations-sorter=alpha
springdoc.swagger-ui.tags-sorter=alpha
springdoc.swagger-ui.display-request-duration=true
springdoc.show-actuator=false

# ── Upload ──
spring.servlet.multipart.max-file-size=4MB
spring.servlet.multipart.max-request-size=4MB
server.tomcat.max-http-form-post-size=5MB

# ── Memoria JVM (Railway free tier ~512MB RAM) ──
# Agrega esto en Railway > Variables de entorno:
# JAVA_TOOL_OPTIONS = -Xmx200m -Xms64m -XX:+UseSerialGC -XX:MaxMetaspaceSize=96m -XX:+TieredCompilation -XX:TieredStopAtLevel=1

# ── Tomcat (reducir hilos para ahorrar RAM) ──
server.tomcat.threads.max=20
server.tomcat.threads.min-spare=2
server.tomcat.accept-count=10

# ── Compresion HTTP (reduce ancho de banda) ──
server.compression.enabled=true
server.compression.mime-types=application/json,application/javascript,text/css,text/html
server.compression.min-response-size=1024